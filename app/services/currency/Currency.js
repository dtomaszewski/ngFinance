'use strict';

angular.module('currency', []).factory('Currency', function ($http) {
        var url = 'https://community-neutrino-currency-conversion.p.mashape.com/convert';

        function convert(fromCurrency, toCurrency, amount) {}

        return {};
});